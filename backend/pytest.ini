[pytest]
# Keep backend imports stable when pytest is invoked from backend/ or CI.
pythonpath = .

# Fixed 2 xdist workers. loadscope keeps each module on one worker.
required_plugins = pytest-xdist
addopts = -n 2 --dist loadscope
